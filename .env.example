# OpenClaw gateway paths (relative to this repo root, or absolute)
OPENCLAW_CONFIG_DIR=.openclaw
OPENCLAW_WORKSPACE_DIR=.openclaw
RKDIR=/path/to/astromech

# Ports and binding
OPENCLAW_GATEWAY_PORT=3000
OPENCLAW_WEBTOKEN_PORT=3001
OPENCLAW_GATEWAY_BIND=127.0.0.1

# Version tag for Docker images (e.g. latest, 1.2.3)
ASTROMECH_VERSION=latest

# Timezone (e.g. Europe/London, America/New_York)
TZ=UTC

# Telegram bot tokens — get from @BotFather, one per agent
TELEGRAM_BOT_BBYODA_TOKEN=YOUR_BOT_TOKEN

# Container user/group IDs — match your host user (run: id -u && id -g)
PUID=1000
PGID=1000

# 1Password service account token (optional — for secrets integration)
OP_SERVICE_ACCOUNT_TOKEN=YOUR_1PASSWORD_TOKEN

# Your public domain (used for reverse proxy, e.g. yourdomain.com)
DOMAIN=yourdomain.com

# Newt tunnel credentials (optional — for Pangolin/Fossorial tunneling)
NEWT_ID=YOUR_NEWT_ID
NEWT_SECRET=YOUR_NEWT_SECRET

// ═══════════════════════════════════════════════════════════════════════════
// 📡 TELEGRAM CHANNELS CONFIGURATION
// ═══════════════════════════════════════════════════════════════════════════
// All Telegram bot accounts for the droid fleet
// Each bot has its own token, policies, and streaming settings
// ═══════════════════════════════════════════════════════════════════════════
// Documentation current as of: OpenClaw v2026.2.17
// Last reviewed: 2026-02-18
// 📄 Docs: /path/to/astromech/tools/wiki/source/docs/gateway/configuration-reference.md:96-154
// 📄 Docs: /path/to/astromech/tools/wiki/source/docs/channels/telegram.md:62-117
// 🤖 Wiki: just wiki-docs "telegram channels configuration"
// 🤖 Wiki: just wiki-docs "telegram bot token setup"

{
  "telegram": {
    // ─────────────────────────────────────────────────────────────────────────
    // Global Telegram Settings
    // ─────────────────────────────────────────────────────────────────────────
    // Get bot tokens from @BotFather on Telegram
    // DM policies: pairing (approval) | allowlist | open | disabled
    // Group policies: allowlist | open | disabled
    // Stream modes: full (every chunk) | partial (coalesced) | none (final only)
    // Ack reactions: Unicode emoji shown while processing (NEW in 2026.2.15)
    //   Resolution order: account → channel → messages.ackReaction → agent emoji
    // 📄 Docs: /path/to/astromech/tools/wiki/source/docs/channels/pairing.md
    // 📄 Docs: /path/to/astromech/tools/wiki/source/docs/channels/groups.md:192-249
    // 📄 Docs: /path/to/astromech/tools/wiki/source/docs/channels/telegram.md:571-626
    // 🤖 Wiki: just wiki-docs "channels dmPolicy pairing"
    // 🤖 Wiki: just wiki-docs "telegram groups requireMention"
    // 🤖 Wiki: just wiki-docs "telegram ackReaction account"
    "enabled": true,
    "dmPolicy": "pairing",
    "groupPolicy": "allowlist",
    "streamMode": "partial",
    "ackReaction": "👀",  // Default fallback for all bots

    // Reaction events (NEW in v2026.2.17)
    // reactionNotifications: off | own | all — which reactions trigger system events
    // reactionLevel: off | ack | minimal | extensive — agent's own reaction capability
    // 📄 Docs: /path/to/astromech/tools/wiki/source/docs/channels/telegram.md:737-738
    "reactionNotifications": "all",
    "reactionLevel": "minimal",

    // ─────────────────────────────────────────────────────────────────────────
    // Bot Accounts
    // ─────────────────────────────────────────────────────────────────────────
    "accounts": {
      // ───────────────────────────────────────────────────────────────────────
      // 🧙 BB-Yoda Bot (Yoda Agent)
      // ───────────────────────────────────────────────────────────────────────
      "yoda": {
        "name": "Master Yoda",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_BBYODA_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "🤔",  // Wise mentor (thinking)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🔧 R2D2 Bot (Default Agent)
      // ───────────────────────────────────────────────────────────────────────
      "r2d2": {
        "name": "R2D2 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_R2D2_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "⚡",  // Versatile astromech droid
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🟠 BB8 Bot
      // ───────────────────────────────────────────────────────────────────────
      "bb8": {
        "name": "BB8 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_BB8_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "🔥",  // Spherical scout droid (energetic)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // ⚫ BB9E Bot
      // ───────────────────────────────────────────────────────────────────────
      "bb9e": {
        "name": "BB9E Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_BB9E_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "😈",  // Dark side companion
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🗣️ C3PO Bot
      // ───────────────────────────────────────────────────────────────────────
      "c3po": {
        "name": "C3PO Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_C3PO_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "🤓",  // Protocol specialist (smart)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🤖 CB23 Bot
      // ───────────────────────────────────────────────────────────────────────
      "cb23": {
        "name": "CB23 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_CB23_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "👻",  // Resistance recon droid (stealth)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🔴 R5D4 Bot
      // ───────────────────────────────────────────────────────────────────────
      "r5d4": {
        "name": "R5D4 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_R5D4_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "❤️",  // Red astromech
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🟡 R4P17 Bot
      // ───────────────────────────────────────────────────────────────────────
      "r4p17": {
        "name": "R4P17 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_R4P17_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "🏆",  // Obi-Wan's companion (champion)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // ⚪ D0 Bot
      // ───────────────────────────────────────────────────────────────────────
      "d0": {
        "name": "D0 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_D0_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "😇",  // Cone-headed helper (innocent)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🎮 L337 Bot
      // ───────────────────────────────────────────────────────────────────────
      "l337": {
        "name": "L337 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_L337_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "😎",  // Self-made pilot droid (cool)
        "capabilities": { "inlineButtons": "dm" }
      },

      // ───────────────────────────────────────────────────────────────────────
      // 🔵 K2S0 Bot
      // ───────────────────────────────────────────────────────────────────────
      "k2s0": {
        "name": "K2S0 Bot",
        "dmPolicy": "pairing",
        "botToken": "${TELEGRAM_BOT_K2S0_TOKEN}",
        "groupPolicy": "allowlist",
        "streamMode": "partial",
        "ackReaction": "👾",  // Reprogrammed Imperial droid (robot alien)
        "capabilities": { "inlineButtons": "dm" }
      }
    }
  }
}

# 2026-02-18

## Lessons
- **Use text commands for Telegram, not HTML charts.** `whoop dashboard`, `whoop summary --color`, `whoop trends` etc. produce clean terminal output that works perfectly in chat. Don't default to HTML chart generation — only use `whoop chart` when explicitly asked for a visual/file.
